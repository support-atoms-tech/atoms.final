#!/usr/bin/env sh
set -e

echo "🔍 Running pre-commit checks..."

# Lint & format only STAGED files
echo "🧹 Formatting & linting staged files with lint-staged..."
bunx lint-staged

echo "✅ Pre-commit checks passed!"
